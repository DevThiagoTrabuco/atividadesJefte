use geriatricare

INSERT INTO usuarios (nome_usuario, senha)
VALUES ('jorge', '$2a$10$BXRHeBV7.URY/sZa0WyrMunoY81lYoGGH.5GTrl5QZkCpV3Y5mR2K');


INSERT INTO papeis (papel) VALUES ('ADMIN'), ('CLIENTE'), ('FUNCIONARIO'), ('FAMILIAR'), ('ATIVADO'), ('DESATIVADO');

INSERT INTO usuarios_papeis (usuario_id, papel_id)
VALUES (
    (SELECT usuario_id FROM usuarios WHERE nome_usuario = 'jorge'), 
    (SELECT papel_id FROM papeis WHERE papel = 'ADMIN')
);

INSERT INTO usuarios_papeis (usuario_id, papel_id)
VALUES (
    (SELECT usuario_id FROM usuarios WHERE nome_usuario = 'jorge'), 
    (SELECT papel_id FROM papeis WHERE papel = 'ATIVADO')
);


select * from usuarios;

select * from usuarios_papeis;

select * from clientes;
